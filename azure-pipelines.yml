trigger:
- none

pool:
  name: Default

steps:


- template: templates/terraform-validate.yml
- template: templates/terraform-plan.yml
    parameters:
    workingDirectory: terraform
    azureSubscription: 'NUMELE_SERVICE_CONNECTION'
  